interface IStudentDal:IRepository<Student>
{
	
}

class Student:IEntity
{
	
}

interface IEntity
{
	//IEntity i implemente eden class lar veri tabanı nesnesidir.
}

interface IRepository<T> where T:class,IEntity, new()
{
	//where ve new generic metoda kısıtlama yeteneği getirir.
	//anlamı class ve new lenebilir bir şey gelecek 
	//new() en sonda olmalı.
	//IEntity eklediğimizde kurala IEntity olması gerektiğini ekler ve IEntity olanları kabul eder.
	//burada sadece referans tipler tutulur
	//Eğer değer tip tutmak istersek o zaman where T:struct yazılmalıdır.
	
}